<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- App theme after splash (Compose owns UI; keep dark status/nav) -->
    <style name="Theme.FindIt" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:colorPrimary">@color/brand_gold</item>
        <item name="android:colorPrimaryDark">@color/splash_background</item>
        <item name="android:colorAccent">@color/brand_green</item>
    </style>

    <!-- Android 12+ SplashScreen API + pre-12 window background -->
    <style name="Theme.FindIt.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.FindIt</item>
        <!-- Pre-12 fallback -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
    </style>

    <!-- Backward-compatible aliases used by older resources -->
    <style name="Theme.MyApplication" parent="Theme.FindIt" />
</resources>
